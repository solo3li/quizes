services:
  web:
    build: .
    volumes:
      - .:/app
      - db_data:/app/db_data
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DEBUG=1
      # Override the DB path if needed via environment variable in settings.py
    restart: always

volumes:
  db_data:
